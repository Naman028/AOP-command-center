{
  "name": "aop-command-center",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Secure AOP Command Center MERN monolith.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "predev": "npm run dev:stop",
    "dev": "concurrently -k -n API,WEB -c blue,green \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server",
    "dev:stop": "node scripts/stop-dev-ports.js",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "mongo:gate": "node scripts/mongodb-persistence-gate.js",
    "security:check": "npm audit --workspaces --audit-level=high",
    "qa:release": "node scripts/qa-release.js",
    "qa:licenses": "node scripts/qa-licenses.js",
    "qa:unused": "node scripts/qa-unused.js",
    "qa:secrets": "node scripts/qa-secrets.js",
    "qa:security-checklist": "node scripts/qa-security-checklist.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "concurrently": "^9.0.0",
    "knip": "^6.25.0"
  },
  "overrides": {
    "uuid": "^11.1.1"
  },
  "engines": {
    "node": ">=22 <23"
  }
}
